import 'package:flutter/material.dart';
class JeweleryScreen extends StatelessWidget {
  const JeweleryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("JeweleryScreen"));
  }
}
